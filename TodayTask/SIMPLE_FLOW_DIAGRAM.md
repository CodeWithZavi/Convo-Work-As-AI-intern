# 📊 SIMPLE APPLICATION FLOW DIAGRAM

```
┌─────────────────────────────────────────────────────────────────┐
│                    START: main.py (Entry Point)                  │
│                      if __name__ == "__main__"                   │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
                   ┌─────────────────────────┐
                   │   app = Main()          │
                   │   Initialize:           │
                   │   • DatabaseManager     │
                   │   • EssayLoader         │
                   │   • Menu                │
                   └───────────┬─────────────┘
                               │
                               ▼
                   ┌───────────────────────────┐
                   │   app.run()               │
                   └───────────┬───────────────┘
                               │
                               ▼
         ┌─────────────────────────────────────────────┐
         │   initialize_database()                     │
         │                                             │
         │   ├─ create_table()                         │
         │   │   └─ PostgreSQL: CREATE TABLE essays   │
         │   │                                         │
         │   ├─ clear_all_essays()                     │
         │   │   └─ PostgreSQL: DELETE all rows       │
         │   │                                         │
         │   ├─ load_essays_from_folder()              │
         │   │   └─ Read essays/*.txt files            │
         │   │                                         │
         │   └─ insert_essay() (loop for each essay)  │
         │       └─ PostgreSQL: INSERT INTO essays    │
         └─────────────────┬───────────────────────────┘
                           │
                           ▼
              ┌────────────────────────────┐
              │   menu.run()               │
              │   Start Menu Loop          │
              └────────┬───────────────────┘
                       │
                       ▼
         ┌─────────────────────────────────────┐
         │   MENU LOOP (while True)            │
         │                                     │
         │   display_menu() - Show Options     │
         │                                     │
         │   Get User Input (1-5)              │
         │   │                                 │
         │   ├─[1] show_all_essays()           │
         │   │      └─ get_all_essays()        │
         │   │         └─ SELECT * FROM essays │
         │   │                                 │
         │   ├─[2] show_essay_by_id()          │
         │   │      └─ get_essay_by_id()       │
         │   │         └─ SELECT WHERE id=?    │
         │   │                                 │
         │   ├─[3] show_title_by_id()          │
         │   │      └─ get_title_by_id()       │
         │   │         └─ SELECT title WHERE id=?│
         │   │                                 │
         │   ├─[4] show_id_by_title()          │
         │   │      └─ get_id_by_title()       │
         │   │         └─ SELECT id WHERE title=?│
         │   │                                 │
         │   └─[5] EXIT                        │
         │         └─ break loop               │
         │                                     │
         └──────────────────┬──────────────────┘
                            │ (when user selects 5)
                            ▼
                 ┌─────────────────────┐
                 │   END PROGRAM       │
                 │   Print "Exiting"   │
                 └─────────────────────┘


═══════════════════════════════════════════════════════════════════
                        FLOW PATH SUMMARY
═══════════════════════════════════════════════════════════════════

START (main.py)
   ↓
Initialize Components (DatabaseManager, EssayLoader, Menu)
   ↓
Setup Database (create table, clear data)
   ↓
Load Essays (read from essays/ folder)
   ↓
Insert Essays (into PostgreSQL database)
   ↓
Menu Loop (display options → get user input → execute action)
   ↓
User Selects Option 5 (Exit)
   ↓
END

═══════════════════════════════════════════════════════════════════
```
